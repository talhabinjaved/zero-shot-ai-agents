# Environment Variables Template
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to git (it's in .gitignore)

# =============================================================================
# REQUIRED FOR ALL PROVIDERS
# =============================================================================

# GitHub personal access token with 'repo' scope
# Get from: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_your_token_here

# Your GitHub username or organization name
GITHUB_OWNER=your-username-or-org

# =============================================================================
# AUGMENT-SPECIFIC (only needed for augment/)
# =============================================================================

# Augment session authentication token
# Get by running: auggie --print-augment-token
# Requires: npm i -g @augmentcode/auggie && auggie --login
AUGMENT_SESSION_AUTH=your_augment_session_token

# =============================================================================
# JULES-SPECIFIC (only needed for jules/)
# =============================================================================

# Jules API key from jules.google Settings
# Get from: https://jules.google → Settings → API Keys
# Requires: Jules GitHub App installed with "All repositories" access
JULES_API_KEY=your_jules_api_key

# =============================================================================
# COSINE-SPECIFIC (only needed for cosine/)
# =============================================================================

# Cosine doesn't require API keys in the orchestrator
# Uses manual import workflow and CLI authentication
# Install CLI: brew install CosineAI/tap/cos
# Authenticate: cos login

# =============================================================================
# OPENHANDS-SPECIFIC (only needed for openhands/)
# =============================================================================

# OpenHands Cloud API key
# Get from: https://app.all-hands.dev → Settings → API Keys
# Requires: OpenHands Cloud GitHub App installed
OPENHANDS_API_KEY=your_openhands_api_key

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Python logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Maximum concurrent operations (default: 3)
# Augment: No hard limit
# Jules: Free=3, Pro=15, Ultra=60
# Cosine: Manual
# OpenHands: Depends on plan
MAX_CONCURRENT=3

# =============================================================================
# NOTES
# =============================================================================

# 1. Never commit this file with actual values to git
# 2. GitHub token needs 'repo' scope for creating repositories
# 3. For Augment, also connect GitHub in Augment web UI
# 4. For Jules, install GitHub App before creating repos
# 5. For Cosine, you'll manually import repos after creation
# 6. For OpenHands, install GitHub App for repo access

# =============================================================================
# LOADING ENVIRONMENT VARIABLES
# =============================================================================

# To use this file in your shell:
# source .env  # bash/zsh
# 
# Or use python-dotenv in scripts (already included in requirements.txt):
# from dotenv import load_dotenv
# load_dotenv()

